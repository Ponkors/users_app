name: data
description: A new Flutter package project.
version: 0.0.1
publish_to: "none"

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  domain:
    path: ../domain

  hive: ^2.2.3
  hive_flutter: ^1.0.0
  equatable: ^2.0.5
  dio: ^5.3.3
  retrofit: ^4.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  retrofit_generator: ^8.0.3

flutter: